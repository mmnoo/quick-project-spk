import { render } from '@testing-library/react'
import App from './App'

test('Im just a test so Husky lets us push to origin. Well delete me soon when real tests exist', () => {
  render(<App />)
})
